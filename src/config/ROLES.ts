export type RolesValues = "employee" | "manager" | "admin";

type Roles = {
  Employee: "employee";
  Manager: "manager";
  Admin: "admin";
};

const ROLES: Roles = {
  Employee: "employee",
  Manager: "manager",
  Admin: "admin",
};

export default ROLES;
